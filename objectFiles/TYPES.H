#ifndef TYPES_H
#define TYPES_H

typedef unsigned char UINT8;
typedef unsigned int  UINT16;
typedef unsigned long UINT32;

typedef enum {
    FALSE = 0,
    TRUE = 1,
}bool;

typedef enum {
    LEFT = -1,
    RIGHT = 1,
};

#endif
